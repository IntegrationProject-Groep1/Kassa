[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "identity-client"
version = "0.1.0"
description = "Identity Service RPC client (packaged copy)"
authors = [
  { name = "Kassa Team" }
]
dependencies = [
  "pika",
  "defusedxml",
]
