services:
  kassa-web:
    image: ${ODOO_IMAGE:-odoo:17.0}
    depends_on:
      - kassa-db
    container_name: kassa_odoo
    restart: unless-stopped
    ports:
      - "8069:8069"
    volumes:
      - ./addons:/mnt/extra-addons
      - ./odoo.conf:/etc/odoo/odoo.conf:ro
      - odoo-data:/var/lib/odoo
    environment:
      - HOST=kassa-db
      - USER=${POSTGRES_USER}
      - PASSWORD=${POSTGRES_PASSWORD}
      - DBNAME=${ODOO_DB}

  kassa-db:
    image: postgres:15
    container_name: kassa_db
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
    volumes:
      - db-data:/var/lib/postgresql/data

  kassa-integratie:
    build: .
    container_name: kassa_integratie
    environment:
      - ODOO_URL=${ODOO_URL}
      - ODOO_DB=${ODOO_DB}
      - ODOO_USER=${ODOO_USER}
      - ODOO_PASS=${ODOO_PASS}
      - RABBIT_HOST=${RABBIT_HOST}
      - RABBIT_PORT=${RABBIT_PORT}
      - RABBIT_USER=${RABBIT_USER}
      - RABBIT_PASS=${RABBIT_PASS}
      - RABBIT_VHOST=${RABBIT_VHOST}
      - RABBIT_EXCHANGE=${RABBIT_EXCHANGE}
      - RABBIT_INCOMING_QUEUE=${RABBIT_INCOMING_QUEUE}
      - POLL_INTERVAL=${POLL_INTERVAL}
      - ODOO_MASTER_PASS=${ODOO_MASTER_PASS}
    volumes:
      - ./integratie:/app
      - outbox-data:/app/outbox
    depends_on:
      - kassa-web
    restart: unless-stopped

volumes:
  outbox-data:
  db-data:
  odoo-data: